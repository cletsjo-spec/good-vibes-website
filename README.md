feat: build Good Vibes website from presentation and add GitHub actions build workflow Created a Vite + React + Tailwind CSS single page application based on the "Good Vibes" pitch deck presentation. Implemented components for Hero, Problem, Solution, Market, BusinessModel, GoToMarket, Team, WhyNow, Investment, and Contact sections using the content extracted from the provided Google Slides link. Also added a `.github/workflows/build.yml` GitHub Actions workflow to build the React application automatically on push and pull requests to the main branch.
